{
  "name": "c0v1d",
  "version": "0.0.1",
  "description": "A game of constant pandemics",
  "homepage": "https://github.com/gungorkocak/c0v1d",
  "main": "index.js",
  "scripts": {
    "start": "npx parcel index.html",
    "build": "npx parcel build index.html --public-url ./",
    "deploy": "npm run build && git add dist && git commit -m 'page build' && hub push && npx gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-plugin-transform-react-jsx": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "babel-preset-hyperapp": "^2.3.0",
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "hyperapp": "^2.0.4",
    "uninstall": "0.0.0"
  }
}
